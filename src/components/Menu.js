import React, { useState } from 'react';
import 'bootstrap';

const Card = () => {
    return (
        <div>
            <nav className="nav nav-tabs-lg bg-primary" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                <h1 className="nav-item" style={{ color: 'white' }}>
                    Basic Pagination Project
                </h1>
            </nav>
        </div>
    );
};

export default Card;